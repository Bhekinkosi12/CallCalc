<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CallCalc.DishPages.FriedEggPage">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="header" FontSize="Large" HorizontalTextAlignment ="Center"  Text="{Binding Source={x:Reference Name=input}, Path=Value}"/>
            <Slider x:Name="input" Minimum="0" Maximum="500" Value="{Binding Path=DishWeight}"/>
            <Button x:Name="button" Text="Посчитать" Command="{Binding Calculate}"/>
            <Label x:Name="output" FontSize="Large" Text="{Binding TotallCall}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>